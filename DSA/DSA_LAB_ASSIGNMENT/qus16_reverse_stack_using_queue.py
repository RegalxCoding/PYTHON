# Initialize an Empty Queue:

# Create an empty queue.
# Transfer Elements from Stack to Queue:

# While the stack is not empty:
# Pop the top element of the stack.
# Enqueue this element into the queue.
# Transfer Elements Back from Queue to Stack:

# While the queue is not empty:
# Dequeue the front element of the queue.
# Push this element back onto the stack.
# The Stack is Now Reversed:

# The stack now contains elements in reverse order compared to the original state.
